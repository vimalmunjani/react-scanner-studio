# Shared component library — owned by the design system team
/src/components/ @org/design-system

# Auth feature — identity team + alice (individual contributor)
/src/features/auth/ @org/identity-team @alice

# Checkout feature — payments team
/src/features/checkout/ @org/payments

# Pages — frontend guild + charlie
/src/pages/ @org/frontend @charlie

# NOTE: src/admin/, src/analytics/, src/notifications/,
#       src/settings/, src/reports/ and src/onboarding/
#       are intentionally left unowned to demonstrate
#       the "many unowned files" edge case in the dashboard.
